Vercel deploy fixes applied:
- Removed next/font/google usage from layout.tsx to avoid build-time font fetch issues.
- Added @types/node and @types/react-dom because Next.js 16 still validates TypeScript setup even when type validation is skipped.
- Removed unsupported eslint config key from next.config.ts.
- Added .npmrc and vercel.json to install without package-lock on Vercel.
- Removed unused example/ui files that referenced packages not needed by this landing page.

Commands:
npm install --no-package-lock
npm run build
